export const screenSize =
  (key) =>
  ({ theme }) =>
    theme.screenSizes[key];
export const colors =
  (key) =>
  ({ theme }) =>
    theme.colors[key];
export const sizes =
  (key) =>
  ({ theme }) =>
    theme.sizes[key];

